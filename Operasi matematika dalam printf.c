#include <stdio.h>
int main()
{
    int a, b;
    printf("Enter two numbers:");
    scanf("%d %d", &a, &b); 
    printf("Sum: %d", a+b); //Operator penjumlahan jika tidak butuh variabel baru
    return 0;
}
